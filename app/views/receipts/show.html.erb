<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @receipt.name %>
</p>

<p>
  <strong>Address:</strong>
  <%= @receipt.address %>
</p>

<p>
  <strong>Email:</strong>
  <%= @receipt.email %>
</p>

<div class="media">
  <div class="media-body">
    <table class="table table-hover">
      <thead>
        <tr>
          <th>Author</th>
          <th>Title</th>
          <th>Description</th>
          <th>Price</th>
          <th>Quantity</th>
          <th>Image url</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% for item in @receipt.cart_items %>
          <tr>
            <td><%= item.item.author %></td>
            <td><%= item.item.title %></td>
            <td><%= item.item.description %></td>
            <td>£<%= item.item.price %></td>
            <td><%= item.quantity %></td>
            <td><%= link_to (image_tag item.item.image_url, :style => 'height:100px'), item %></td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>


<!--<%= link_to 'Edit', edit_receipt_path(@receipt) %> |-->
<%= link_to 'Back', receipts_path %>
